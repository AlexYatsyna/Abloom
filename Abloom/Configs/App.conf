akka {

	loglevel = ERROR
	
	actor
    {
      provider = "Akka.Cluster.ClusterActorRefProvider, Akka.Cluster"
    }

	remote {
	    dot-netty.tcp {
		    port = 5800 
			hostname = "localhost"
		}
	}
	
	cluster {
		seed-nodes = ["akka.tcp://msys@localhost:5800"]
	}
}
